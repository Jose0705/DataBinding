<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Databalding.Coleccion.NewFolder"
             x:Class="Databalding.Coleccion.Views.MainPage">

    <Grid 
        BackgroundColor="White"
        RowDefinitions="Auto, *, Auto"
        Padding="16">
        
        <Grid
        ColumnDefinitions="*, Auto, Auto" 
        ColumnSpacing="4">

            <Label
            Text="Origenes del paquete" />
            
            <Button
            BorderWidth="1"
            BorderColor="Green"
            Grid.Column="1"
            Text="+" 
            TextColor="Green"
            Clicked="OnAgregarButtonCliked"
            BackgroundColor="White"/>
            
            <Button
            BorderWidth="1"
            BorderColor="red"
            TextColor="red"
            Clicked="OnDelateButtonCliked"
            BackgroundColor="White"
            Grid.Column="2"
            Text="X"/>
            
        </Grid>

        <CollectionView
            Grid.Row="1"
            x:Name="OrigenesListView"
            SelectionMode="Single"
            EmptyView="Aun no se registran origenes de paquetes"
            SelectionChanged="OrigenesListView_SelectionChanged"
            BackgroundColor="Beige">
            
            <!--<CollectionView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type models:OrigenDePaquete}">
                    
                <Border
                Stroke="Blue"
                Padding="8">
                        <Grid
                            RowDefinitions="auto"
                            ColumnDefinitions="auto,auto">
                            <CheckBox
                                Grid.RowSpan="2"
                                IsChecked="False"/>
                            <Label
                                Grid.Column="1"
                                Text="Empty"
                                />
                            <Label
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="Empty 2" />
                        </Grid>
                </Border>
                
                </DataTemplate>
            </CollectionView.ItemTemplate>-->
            
            
            
            <!--<ListView.ItemsSource>
            <x:Array Type="{x:Type x:String}">
            <x:String>Superman</x:String>
            <x:String>Thor</x:String>
            <x:String>Batman</x:String>
            <x:String>Spiderman</x:String>

            </x:Array>
            </ListView.ItemsSource>-->
            <!--

            <ListView.ItemTemplate>
            <DataTemplate>
            <Label
            Text=""
            FontSize="16"/>
            </DataTemplate>
            </ListView.ItemTemplate>-->

        </CollectionView>

        <Grid
        Grid.Row="2"
        RowDefinitions="Auto, Auto, Auto"
        ColumnDefinitions="Auto, *, Auto">

            <Label
            Text="Nombre:" 
            VerticalOptions="Center"/>
            <Entry
                x:Name="NombreEntry"
          Grid.Column="1" />

            <Label
            Grid.Row="1"
            Text="Origen:"
            VerticalOptions="Center"/>
            <Entry
            Grid.Row="1"
                BackgroundColor="Gray"
                x:Name="OrigenEntry"
            Grid.Column="1" />

            <Button
            TextColor="Black"
            BorderWidth="1"
            BorderColor="Black"
            BackgroundColor="White"
            Grid.Row="1"
            Grid.Column="2"
                Clicked="OnActualizarButton_Cliked"
            Text="Actualizar"/>
            
        </Grid>
        
    </Grid>

</ContentPage>
